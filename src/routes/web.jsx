import Layout from "../Front/layout";

const routes = {
  path: "/",
  element: <Layout />,
  children: [
    // { index: true, element: <Home /> },
    // { path: "login", element: <Login /> },
  ],
}

export default routes